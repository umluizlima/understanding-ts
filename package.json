{
  "name": "understanding-ts",
  "version": "1.0.0",
  "scripts": {
    "compile": "tsc app.ts",
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "typescript": "^4.5.5"
  }
}
